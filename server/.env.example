# Copy this file to .env and fill in your API key
GEMINI_KEY=YOUR_GOOGLE_GENERATIVE_API_KEY_HERE
# Optional: PORT the proxy should listen on (default 4000)
# PORT=4000
 
# SMTP settings for sending hire emails (used by /api/hire)
# Example for Gmail SMTP via App Passwords or other SMTP provider
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-smtp-username@example.com
# SMTP_PASS=your-smtp-password-or-app-password
# SMTP_SECURE=false # set to true for port 465, false for 587

# The email address that should receive incoming hire requests
# Default is mdirreef@gmail.com if not set
# HIRE_TO_EMAIL=mdirreef@gmail.com

# SMTP settings for sending hire emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=mdirreef@gmail.com
SMTP_PASS=nhaspxbtmijoafdn
SMTP_SECURE=false

# Recipient of hire requests (defaults to mdirreef@gmail.com if unset)
HIRE_TO_EMAIL=mdirreef@gmail.com